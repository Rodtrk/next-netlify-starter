{
  "compilerOptions": {
    "baseUrl": "./",
    "paths": {
      "@components/*": ["components/*"],
      "@styles/*": ["styles/*"]
    }
  }
}

async Headers(){
  return [
    {
      source : "/(.*)",
      headers :
      {
        key : "X-Content-Type-Options",
        value : "nosniff"
      }
  ]
}
